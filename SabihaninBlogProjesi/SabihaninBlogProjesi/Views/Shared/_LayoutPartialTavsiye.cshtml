@model IEnumerable<DomainEntity.Models.TavsiyePartialModeli>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TavsiyePartialModeliID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TavsiyePartialModeliID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TavsiyePartialModeliID })
        </td>
    </tr>
}

</table>
